{
  "name": "baseballalone",
  "private": true,
  "type": "module",
  "description": "정적 웹 게임 — probe.mjs 실행을 위해 ESM 모드만 지정. 빌드/의존성 없음.",
  "scripts": {
    "probe": "node probe.mjs",
    "probe:career": "node probe-career.mjs",
    "probe:regression": "node probe-regression.mjs"
  }
}
